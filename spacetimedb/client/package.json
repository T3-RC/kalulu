{
  "name": "@kalulu/client",
  "version": "1.0.0",
  "description": "Shared SpacetimeDB client wrapper + hooks for Kalulu (web + mobile)",
  "type": "module",
  "main": "src/kalulu.ts",
  "scripts": {
    "generate": "spacetime generate --lang typescript --out-dir src/module_bindings --module-path ../server",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "spacetimedb": "^2.4.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "//": "Keep this version matching your `spacetime --version` (CLI = codegen). Node 18-21 also need `undici`."
}
